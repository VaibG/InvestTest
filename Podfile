# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'ScenarioTester' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ScenarioTester
  pod 'Alamofire', '~> 4.0'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SkyFloatingLabelTextField', '~> 3.0'
  pod 'Charts'

  target 'ScenarioTesterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ScenarioTesterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
